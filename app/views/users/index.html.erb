<h2>All Users</h2>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
      <% if @acceptable_users.include?(user) %>Accept friendship request?
      <% elsif @friends.include?(user) %>You are friends.
      <% elsif @pending_users.include?(user) %>Your request is pending.
      <% elsif @rejecting_users.include?(user) %>Your request has been rejected.
      <% else %>Send a friendship request?
      <% end %>
    </li>
  <% end %>
</ul>

<p><%= link_to "My Profile", current_user %></p>

<%= render 'common/devise_links' %>
